
import 'package:flutter/material.dart';

class AppBarWidges extends StatelessWidget {
  const AppBarWidges({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
